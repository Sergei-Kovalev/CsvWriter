rootProject.name = "csv"

//include("subproject")
//project("subproject").projectDir = file("somewhere")

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}